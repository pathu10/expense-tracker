server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Support React Router-style client-side routing (not used yet, but
    # this is the standard SPA fallback so refreshing any route works).
    location / {
        try_files $uri $uri/ /index.html;
    }
}
